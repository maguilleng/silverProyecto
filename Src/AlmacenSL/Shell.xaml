<UserControl x:Class="AlmacenSL.Shell"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="clr-namespace:Microsoft.Practices.Prism.Regions;assembly=Microsoft.Practices.Prism"
    xmlns:views="clr-namespace:AlmacenSL.Views"
    FontFamily="/AlmacenSL.Infrastructure;component/Assets/Fonts/TCM.TTF#Tw Cen MT"
    mc:Ignorable="d"
    d:DesignHeight="300" d:DesignWidth="400">
    
    <Grid x:Name="LayoutRoot" Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <!--FontFamily="/Infrastructure;component/Assets/Fuentes/TCM.TTF#Tw Cen MT"-->
        <StackPanel>
            <views:LoginUserControl/>
            <!--<Button x:Name="loadButtton" Command="{Binding LoadModuleCommand}" Content="Cargar Módulo"/>-->
        </StackPanel>
        
        
        <ContentControl prism:RegionManager.RegionName="MainContentRegion" Grid.Row="1"
                        HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"/>
    </Grid>
</UserControl>
